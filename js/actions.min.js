window.addEventListener('scroll', scrollFunc);

function scrollFunc() {
    var windowScroll = this.scrollY;

    var $logo = document.getElementsByClassName('home-parallax-1')[0];
    $logo.style.transform = 'translateY(' - windowScroll / 45 + '%)';

    var $backBird = document.getElementsByClassName('home-parallax-2')[0];
    $backBird.style.transform = 'translateY(' + windowScroll / 8 + '%)';
    //$backBird.style.opacity = 1 - (windowScroll / 1000) * 3;

    var $backBird = document.getElementsByClassName('home-parallax-3')[0];
    $backBird.style.transform = 'translateY(' + windowScroll / 6 + '%)';
    //$backBird.style.opacity = 1 - (windowScroll / 1000) * 3;

    var $logo = document.getElementsByClassName('home-parallax-4')[0];
    $logo.style.transform = 'translateY(' - windowScroll / 6 + '%)';

    var $backBird = document.getElementsByClassName('home-parallax-5')[0];
    $backBird.style.transform = 'translateY(' + windowScroll / 40 + '%)';
    //$backBird.style.opacity = 1 - (windowScroll / 1000) * 3;


    console.log(windowScroll + "+" + $backBirdLength);
}